language: python

python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"

install:
    - pip install codecov
    - pip install -r requirements.txt
    - pip install -e .

script: py.test --cov

after_success:
    - codecov
