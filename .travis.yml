language: python
sudo: false

python:
  - '3.4.3'
  - '3.5.0'

env:
  - TOXENV=py34
  - TOXENV=py35

install:
  - pip install tox

script:
  - tox

after_success:
  - pip install coverage coveralls
  - coverage report
  - coveralls
